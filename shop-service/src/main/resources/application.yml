spring:
  application:
    name: shop-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: development
  cloud:
    config:
      label: 005-HTTP-communication
